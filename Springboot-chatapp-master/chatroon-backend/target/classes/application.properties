# Server port
server.port=8080

# Application name
spring.application.name=chatroon-backend

# WebSocket path
spring.websocket.path=/ws

# WebSocket STOMP endpoint allowed origin (optional security config)
spring.websocket.allowed-origins=*

# Logging level (optional for debugging)
logging.level.org.springframework=INFO


